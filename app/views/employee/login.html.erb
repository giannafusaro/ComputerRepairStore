<%= form_for :employee, url: employee_login_path do |f| %>
  <%= f.label :login %>
  <%= f.text_field :login %>

  <%= f.label :password %>
  <%= f.password_field :password %>

  <br />
  <%= f.submit 'Submit', class: 'btn' %>
<% end %>
